apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: wordpress
  name: demo-pvc-claim
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi